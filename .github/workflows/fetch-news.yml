name: Fetch Crypto News & Price Analysis

on:
  schedule:
    # 每天 8:00 和 20:00 (北京时间) 运行
    - cron: '0 0,12 * * *'  # UTC 0:00 和 12:00 = 北京 8:00 和 20:00
  workflow_dispatch:  # 允许手动触发

jobs:
  fetch-news-and-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      
      - name: Install dependencies
        run: pip install requests
      
      - name: Run fetch_news.py (资讯抓取)
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/fetch_news.py
      
      - name: Run price_analysis.py (价格分析)
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/price_analysis.py
      
      - name: Commit and push new content
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add src/content/zh/news/ src/content/en/news/ || true
          git add src/content/zh/analysis/ src/content/en/analysis/ || true
          if git diff --cached --quiet; then
            echo "No new content to commit"
          else
            git commit -m "auto: fetch news & price analysis $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
