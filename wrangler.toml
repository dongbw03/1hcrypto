# Cloudflare Pages deployment config for 1hcrypto.com
name = "1hcrypto"
compatibility_date = "2025-01-01"

# Build output directory
pages_build_output_dir = "dist"

# KV namespace for email subscribers (暂未启用，需要时取消注释并填写真实 ID)
# [[kv_namespaces]]
# binding = "SUBSCRIBERS"
# id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

# Environment variables (non-secret)
[vars]
SITE_URL = "https://1hcrypto.com"
